# Changesets

Every user-visible package change must include a changeset. Run `npm run changeset`,
select the affected packages, choose the SemVer impact, and describe the migration in
consumer terms. Documentation-only and test-only changes do not require one.
